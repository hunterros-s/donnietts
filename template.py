"""Re-export of the package rendering module (legacy shim)."""

from donnietts.rendering import (
    AVAILABLE_FIELDS,
    DEFAULT_TEMPLATE,
    normalize_text,
    render_template,
    template_fields,
)
